spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/job_management
    username: ahmtrdm
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    org.springframework.security: ERROR
    com.jobmanagement: ERROR